services:
  find-my-house:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_URL: file:/data/listings.db
    volumes:
      - find-my-house-data:/data

volumes:
  find-my-house-data:
