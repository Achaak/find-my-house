services:
  find-my-house:
    build: .
    restart: unless-stopped
    ports:
      - "8099:8099"
    env_file:
      - .env
      - .env.local
    environment:
      DATABASE_URL: file:/data/listings.db
    volumes:
      - find-my-house-data:/data

volumes:
  find-my-house-data:
