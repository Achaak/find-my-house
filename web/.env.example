# Optional: Home Assistant long-lived token for local web dev (pnpm dev:web)
# When opened via HA Ingress, authentication is handled automatically — no token needed.
# VITE_HA_TOKEN=

# Optional: point to a local HA instance when validating tokens outside the add-on
# HOME_ASSISTANT_URL=http://127.0.0.1:8123
